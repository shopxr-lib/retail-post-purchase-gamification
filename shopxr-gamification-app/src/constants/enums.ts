export enum PrizeType {
  Coupon = 'coupon',
  FreeProduct = 'free product',
  CustomPrize = 'custom prize',
  NoPrize = 'no prize'
}

export enum ScreenPosition {
  Left = 'left',
  Right = 'right'
}